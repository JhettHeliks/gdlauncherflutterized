import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0B0F19);
  static const Color sidebarBackground = Color(0xFF121827);
  static const Color surface = Color(0xFF1A233A);
  static const Color searchBarBackground = Color(0xFF151B27);
  
  // Accents
  static const Color primaryAccent = Color(0xFF87A1FA); // Buttons, active states
  static const Color secondaryAccent = Color(0xFF49BAE7); // Glows, secondary indicators
  static const Color premiumGold = Color(0xFFE8B923); // Premium member badge
  static const Color actionCyan = Color(0xFF00E5FF); // Action positive, new instance button
  
  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8B949E);
  static const Color textMuted = Color(0xFF5A6679);

  // States
  static const Color dividerColor = Color(0xFF232D42);
}
